sudo: false

cache:
  rust: true
  directories:
    $HOME/.cargo/bin

language: rust

addons:
  apt:
    packages:
      - libcurl4-openssl-dev
      - libelf-dev
      - libdw-dev
      - binutils-dev
      - cmake
    sources:
      - kalakris-cmake

rust:
  - nightly

env:
  - PATH=$HOME/.cargo/bin:$PATH

script:
  - cargo test
